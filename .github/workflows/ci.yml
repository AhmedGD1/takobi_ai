name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check plugin exists
        run: test -f addons/takobi_ai/plugin.cfg

      - name: Check runtime folder exists
        run: test -d addons/takobi_ai
